# Create Pull Request

Use bash tool to create a GitHub PR.

## Task

The repo root is: /home/laeq/code/harness/ai-workflow
The branch is: workflow-test-branch

## Execute with bash tool:

`cd /home/laeq/code/harness/ai-workflow && gh pr create --title "Test PR from Workflow" --body "This PR was created by the ai-workflow system using opencode executor"`

## Output

Use write tool to create `pr-url.txt` with the PR URL from the gh pr create output.