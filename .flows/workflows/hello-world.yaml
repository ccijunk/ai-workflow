version: "1"

nodes:
  hello:
    role: tester
    prompt: prompts/hello.md
    executor: opencode
    inputs: {}
    outputs: {message: message.txt}

transitions:
  - from: __start__
    to: hello

  - from: hello
    to: __end__